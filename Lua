-- // Services
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- // Cleanup Old UI
local oldUI = (gethui and gethui():FindFirstChild("FatalityNativeMobile")) or CoreGui:FindFirstChild("FatalityNativeMobile") or LocalPlayer.PlayerGui:FindFirstChild("FatalityNativeMobile")
if oldUI then oldUI:Destroy() end

local oldToggle = (gethui and gethui():FindFirstChild("FatalityMobileToggleBtn")) or CoreGui:FindFirstChild("FatalityMobileToggleBtn") or LocalPlayer.PlayerGui:FindFirstChild("FatalityMobileToggleBtn")
if oldToggle then oldToggle:Destroy() end

-- // Global States
getgenv().AimbotEnabled = false
getgenv().AimbotFOV = 120
getgenv().DesyncAntiAim = false
getgenv().PitchAngle = -80

-- // ScreenGui Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FatalityNativeMobile"
ScreenGui.Parent = (gethui and gethui()) or CoreGui or LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- // Main Window Container (Scaled Compact for Mobile)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 12, 22)
MainFrame.BorderColor3 = Color3.fromRGB(235, 30, 90)
MainFrame.BorderSizePixel = 1
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.Size = UDim2.new(0, 320, 0, 200)
MainFrame.Active = true
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

-- // Header Bar
local Header = Instance.new("Frame")
Header.Parent = MainFrame
Header.BackgroundColor3 = Color3.fromRGB(10, 8, 16)
Header.Size = UDim2.new(1, 0, 0, 32)

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 8)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.Parent = Header
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 10, 0, 0)
Title.Size = UDim2.new(0, 150, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "FATALITY<font color=\"rgb(235, 30, 90)\">.WIN</font>"
Title.RichText = true
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left

-- // Header Close Button (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = Header
CloseBtn.BackgroundColor3 = Color3.fromRGB(235, 30, 90)
CloseBtn.Position = UDim2.new(1, -26, 0, 4)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 12
CloseBtn.ZIndex = 10

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 4)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui.Enabled = false
end)

-- // Floating Mobile Re-open Button (F)
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "FatalityMobileToggleBtn"
ToggleGui.Parent = (gethui and gethui()) or CoreGui or LocalPlayer:WaitForChild("PlayerGui")

local ReopenBtn = Instance.new("TextButton")
ReopenBtn.Parent = ToggleGui
ReopenBtn.BackgroundColor3 = Color3.fromRGB(15, 12, 22)
ReopenBtn.BorderColor3 = Color3.fromRGB(235, 30, 90)
ReopenBtn.BorderSizePixel = 1
ReopenBtn.Position = UDim2.new(0.04, 0, 0.25, 0)
ReopenBtn.Size = UDim2.new(0, 40, 0, 40)
ReopenBtn.Font = Enum.Font.GothamBold
ReopenBtn.Text = "F"
ReopenBtn.TextColor3 = Color3.fromRGB(235, 30, 90)
ReopenBtn.TextSize = 18
ReopenBtn.Active = true
ReopenBtn.Draggable = true

local ReopenCorner = Instance.new("UICorner")
ReopenCorner.CornerRadius = UDim.new(0, 8)
ReopenCorner.Parent = ReopenBtn

ReopenBtn.MouseButton1Click:Connect(function()
    ScreenGui.Enabled = not ScreenGui.Enabled
end)

-- // Content Scroll Container
local Scroll = Instance.new("ScrollingFrame")
Scroll.Parent = MainFrame
Scroll.BackgroundTransparency = 1
Scroll.Position = UDim2.new(0, 8, 0, 38)
Scroll.Size = UDim2.new(1, -16, 1, -44)
Scroll.CanvasSize = UDim2.new(0, 0, 0, 160)
Scroll.ScrollBarThickness = 2

local UIList = Instance.new("UIListLayout")
UIList.Parent = Scroll
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 6)

-- // Helper: Create Native Toggle Element
local function AddToggle(text, default, callback)
    local Frame = Instance.new("Frame")
    Frame.Parent = Scroll
    Frame.BackgroundColor3 = Color3.fromRGB(22, 18, 30)
    Frame.Size = UDim2.new(1, 0, 0, 30)

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 4)
    Corner.Parent = Frame

    local Label = Instance.new("TextLabel")
    Label.Parent = Frame
    Label.BackgroundTransparency = 1
    Label.Position = UDim2.new(0, 8, 0, 0)
    Label.Size = UDim2.new(0.7, 0, 1, 0)
    Label.Font = Enum.Font.Gotham
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(220, 220, 230)
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left

    local Btn = Instance.new("TextButton")
    Btn.Parent = Frame
    Btn.BackgroundColor3 = default and Color3.fromRGB(235, 30, 90) or Color3.fromRGB(35, 30, 45)
    Btn.Position = UDim2.new(1, -42, 0.5, -9)
    Btn.Size = UDim2.new(0, 34, 0, 18)
    Btn.Text = default and "ON" or "OFF"
    Btn.Font = Enum.Font.GothamBold
    Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Btn.TextSize = 9

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 4)
    BtnCorner.Parent = Btn

    local state = default
    Btn.MouseButton1Click:Connect(function()
        state = not state
        Btn.Text = state and "ON" or "OFF"
        Btn.BackgroundColor3 = state and Color3.fromRGB(235, 30, 90) or Color3.fromRGB(35, 30, 45)
        callback(state)
    end)
end

-- // Add Native Features
AddToggle("Enable Aimbot", false, function(s) getgenv().AimbotEnabled = s end)
AddToggle("Enable Desync Anti-Aim", false, function(s) getgenv().DesyncAntiAim = s end)

-- // Anti-Aim Engine Loop
RunService.Stepped:Connect(function()
    if not getgenv().DesyncAntiAim then return end
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local root = char.HumanoidRootPart
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum and hum.Health > 0 then
            root.RotVelocity = Vector3.new(0, 0, 0)
            if hum.RigType == Enum.HumanoidRigType.R6 and char:FindFirstChild("Torso") then
                char.Torso.RootJoint.C0 = CFrame.new(0, 0, 0) * CFrame.Angles(-math.pi/2 + math.rad(getgenv().PitchAngle), math.rad(math.random(-90, 90)), math.pi)
            end
        end
    end
end)
